<UserControl x:Class="QUAN_LY.View.AdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000"
             Background="White">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="StandardBoolToVisConverter"/>

        <Style x:Key="AdminItemStyle" TargetType="ListViewItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Margin" Value="0,4"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Border x:Name="Border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="#E0E0E0" 
                                BorderThickness="0,0,0,1" 
                                CornerRadius="8"
                                Padding="10">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#F5F7FA"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#E3F2FD"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="#2196F3"/>
                                <Setter TargetName="Border" Property="BorderThickness" Value="1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Quản Lý Nhân Viên" FontSize="24" FontWeight="Bold" Foreground="#333" VerticalAlignment="Center"/>

            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <materialDesign:PackIcon Kind="Magnify" Width="25" Height="25" Foreground="Gray" VerticalAlignment="Bottom" Margin="0,0,10,5"/>

                <TextBox Width="300" 
                         materialDesign:HintAssist.Hint="Tìm kiếm theo tên hoặc tài khoản..."
                         Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         VerticalAlignment="Center"/>
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" Background="White" BorderBrush="#eee" BorderThickness="1" CornerRadius="10" Padding="10">
            <Grid>
                <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}" 
                             Value="0" IsIndeterminate="True" 
                             HorizontalAlignment="Center" VerticalAlignment="Center"
                             Visibility="{Binding IsLoading, Converter={StaticResource StandardBoolToVisConverter}}"/>

                <ListView ItemsSource="{Binding AdminList}" 
                          SelectedItem="{Binding SelectedAdmin}"
                          ItemContainerStyle="{StaticResource AdminItemStyle}"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled">

                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="60"/>
                                    <ColumnDefinition Width="250"/>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <materialDesign:PackIcon Kind="AccountCircle" 
                                                         Width="40" Height="40" 
                                                         Foreground="#623ed0" 
                                                         VerticalAlignment="Center"/>

                                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0">
                                    <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="16"/>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="ID: " FontSize="12" Foreground="Gray"/>
                                        <TextBlock Text="{Binding AdminId}" FontSize="12" Foreground="Gray"/>
                                    </StackPanel>
                                </StackPanel>

                                <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                    <TextBlock Text="Tài khoản" FontSize="10" Foreground="Gray"/>
                                    <TextBlock Text="{Binding Username}" FontWeight="SemiBold"/>
                                </StackPanel>

                                <Border Grid.Column="3" CornerRadius="10" Background="#E0F7FA" 
                                        HorizontalAlignment="Left" Padding="10,4" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Role}" Foreground="#006064" FontWeight="Bold"/>
                                </Border>

                                <StackPanel Grid.Column="4" Orientation="Horizontal" VerticalAlignment="Center">
                                    <materialDesign:PackIcon Kind="CircleMedium" Width="15" Height="15" Margin="0,2,5,0">
                                        <materialDesign:PackIcon.Style>
                                            <Style TargetType="materialDesign:PackIcon">
                                                <Setter Property="Foreground" Value="Green"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                        <Setter Property="Foreground" Value="Red"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </materialDesign:PackIcon.Style>
                                    </materialDesign:PackIcon>

                                    <TextBlock VerticalAlignment="Center">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Text" Value="Đang hoạt động"/>
                                                <Setter Property="Foreground" Value="Green"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                        <Setter Property="Text" Value="Ngừng hoạt động"/>
                                                        <Setter Property="Foreground" Value="Red"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Xóa Nhân Viên" 
                    Command="{Binding DeleteCommand}"
                    Background="#FF5252" BorderThickness="0" Margin="0,0,10,0"/>

            <Button Content="Thêm Mới (+)" 
                    Command="{Binding OpenAddCommand}"
                    Background="#623ed0" BorderThickness="0"/>
        </StackPanel>

        <Grid Grid.Row="0" Grid.RowSpan="3" 
              Panel.ZIndex="999"
              Background="#CC000000" 
              Visibility="{Binding IsAdding, Converter={StaticResource StandardBoolToVisConverter}}">

            <Border Background="White" Width="400" VerticalAlignment="Center" CornerRadius="15" Padding="20"
                    MouseDown="Border_MouseDown">
                <StackPanel>
                    <TextBlock Text="Thêm Nhân Viên Mới" FontSize="20" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center"/>

                    <TextBox Text="{Binding NewAdmin.Name, UpdateSourceTrigger=PropertyChanged}"
                             materialDesign:HintAssist.Hint="Họ và tên"
                             Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="0,0,0,15"/>

                    <TextBox Text="{Binding NewAdmin.Username, UpdateSourceTrigger=PropertyChanged}"
                             materialDesign:HintAssist.Hint="Tên đăng nhập"
                             Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="0,0,0,15"/>

                    <PasswordBox x:Name="txtNewPassword"
                                 materialDesign:HintAssist.Hint="Mật khẩu (tối thiểu 6 ký tự)"
                                 Style="{StaticResource MaterialDesignFloatingHintPasswordBox}" Margin="0,0,0,15"
                                 PasswordChanged="PasswordBox_PasswordChanged"/>

                    <ComboBox SelectedValue="{Binding NewAdmin.Role}"
                              SelectedValuePath="Content"
                              materialDesign:HintAssist.Hint="Chức vụ"
                              Style="{StaticResource MaterialDesignFloatingHintComboBox}" Margin="0,0,0,20">
                        <ComboBoxItem Content="Sale Staff"/>
                        <ComboBoxItem Content="Manager"/>
                    </ComboBox>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                        <Button Content="Hủy" Command="{Binding CancelAddCommand}" 
                                Style="{StaticResource MaterialDesignFlatButton}" Margin="0,0,10,0"/>

                        <Button Content="Lưu" Command="{Binding SaveNewCommand}" Width="100"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>