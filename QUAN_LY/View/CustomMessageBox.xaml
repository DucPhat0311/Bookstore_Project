<Window x:Class="QUAN_LY.View.CustomMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Thông báo" Height="Auto" Width="400"
        SizeToContent="Height"
        WindowStyle="None" ResizeMode="NoResize"
        AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" Topmost="True"
        FontFamily="Segoe UI">

    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryColor" Color="#2C3E50"/>
        <SolidColorBrush x:Key="AccentColor" Color="#3498DB"/>
        <SolidColorBrush x:Key="SuccessColor" Color="#27AE60"/>
        <SolidColorBrush x:Key="DangerColor" Color="#E74C3C"/>

        <Style x:Key="DialogBtnStyle" TargetType="Button">
            <Setter Property="Height" Value="35"/>
            <Setter Property="Width" Value="100"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="12" Background="White" Margin="20">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.2" Color="Black"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" MouseLeftButtonDown="Header_MouseDown" Background="Transparent">
                <TextBlock x:Name="txtTitle" Text="Thông báo" 
                           Foreground="{StaticResource PrimaryColor}" 
                           FontWeight="Bold" FontSize="16" 
                           Margin="20,15,0,0" VerticalAlignment="Center"/>
            </Grid>

            <Grid Grid.Row="1" Margin="20,20,20,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Width="45" Height="45" CornerRadius="25" Background="#F0F0F0" VerticalAlignment="Top">
                    <Path x:Name="iconPath" Width="20" Height="20" Stretch="Uniform" Fill="{StaticResource PrimaryColor}" 
                          HorizontalAlignment="Center" VerticalAlignment="Center"
                          Data="M12,2A10,10 0 1,0 22,12A10,10 0 0,0 12,2M12,17A1,1 0 1,1 13,16A1,1 0 0,1 12,17M12,13A1,1 0 0,1 11,12V7A1,1 0 0,1 13,7V12A1,1 0 0,1 12,13Z"/>
                </Border>

                <TextBlock x:Name="txtMessage" Grid.Column="1" 
                           Text="Nội dung..." 
                           TextWrapping="Wrap" FontSize="14" Foreground="#555"
                           Margin="15,2,0,0" VerticalAlignment="Center" LineHeight="20"/>
            </Grid>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,20,20">
                <Button x:Name="btnCancel" Content="Hủy bỏ" Click="btnCancel_Click"
                        Style="{StaticResource DialogBtnStyle}"
                        Background="#ECF0F1" Foreground="{StaticResource PrimaryColor}"
                        Visibility="Collapsed"/>

                <Button x:Name="btnOK" Content="Đồng ý" Click="btnOK_Click"
                        Style="{StaticResource DialogBtnStyle}"
                        Background="{StaticResource AccentColor}" Foreground="White"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>