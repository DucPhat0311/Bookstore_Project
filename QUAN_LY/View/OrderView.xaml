<UserControl x:Class="QUAN_LY.View.OrderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QUAN_LY.View"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1280"
             FontFamily="Segoe UI">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <SolidColorBrush x:Key="PrimaryColor" Color="#34495E"/>
        <SolidColorBrush x:Key="AccentColor" Color="#3498DB"/>
        <SolidColorBrush x:Key="SuccessColor" Color="#27AE60"/>
        <SolidColorBrush x:Key="DangerColor" Color="#E74C3C"/>
        <SolidColorBrush x:Key="BgColor" Color="#F4F6F7"/>

        <Style TargetType="ComboBox" x:Key="FilterCombo">
            <Setter Property="Width" Value="150"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="{StaticResource BgColor}">
        <TabControl Margin="20,10,20,20" BorderThickness="0" Background="Transparent">

            <TabItem Header="BÁN HÀNG">
                <Grid Margin="0,15,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2.3*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>

                    <Grid Grid.Column="0" Margin="0,0,20,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Margin="0,0,0,15">
                            <DockPanel Margin="0,0,0,10">
                                <TextBlock DockPanel.Dock="Left" Text="CỬA HÀNG SÁCH" FontSize="24" FontWeight="Bold" Foreground="{StaticResource PrimaryColor}"/>
                                <Border DockPanel.Dock="Right" Background="White" CornerRadius="20" Width="250" Height="40">
                                    <Grid>
                                        <TextBlock Text="🔍 Tìm tên sách..." Foreground="#CCC" VerticalAlignment="Center" Margin="15,0" IsHitTestVisible="False">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding SearchKeyword}" Value="">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding SearchKeyword}" Value="{x:Null}">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                        <TextBox Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}" 
                                                 Background="Transparent" BorderThickness="0" 
                                                 VerticalContentAlignment="Center" Padding="15,0" FontSize="14"/>
                                    </Grid>
                                </Border>
                            </DockPanel>

                            <DockPanel LastChildFill="False">
                                <ComboBox Style="{StaticResource FilterCombo}" ItemsSource="{Binding SubjectList}" SelectedItem="{Binding SelectedSubject, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" DisplayMemberPath="Name">
                                    <ComboBox.Template>
                                        <ControlTemplate TargetType="ComboBox">
                                            <Grid>
                                                <Border Background="White" CornerRadius="8">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition/>
                                                            <ColumnDefinition Width="20"/>
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock Text="📂 Thể loại" Foreground="Gray" VerticalAlignment="Center" Margin="10,0" IsHitTestVisible="False">
                                                            <TextBlock.Style>
                                                                <Style TargetType="TextBlock">
                                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding SelectedIndex, RelativeSource={RelativeSource TemplatedParent}}" Value="-1">
                                                                            <Setter Property="Visibility" Value="Visible"/>
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </TextBlock.Style>
                                                        </TextBlock>
                                                        <ContentPresenter Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" VerticalAlignment="Center" Margin="10,0"/>
                                                        <TextBlock Grid.Column="1" Text="▼" FontSize="10" VerticalAlignment="Center" Foreground="Gray"/>
                                                    </Grid>
                                                </Border>
                                                <Popup Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                                    <Grid Name="DropDown" SnapsToDevicePixels="True" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                                        <Border Name="DropDownBorder" Background="White" BorderThickness="1" BorderBrush="#DDD" CornerRadius="5"/>
                                                        <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                                            <StackPanel IsItemsHost="True"/>
                                                        </ScrollViewer>
                                                    </Grid>
                                                </Popup>
                                                <ToggleButton IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" Opacity="0"/>
                                            </Grid>
                                        </ControlTemplate>
                                    </ComboBox.Template>
                                </ComboBox>

                                <ComboBox Style="{StaticResource FilterCombo}" ItemsSource="{Binding AuthorList}" SelectedItem="{Binding SelectedAuthor, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" DisplayMemberPath="Name">
                                    <ComboBox.Template>
                                        <ControlTemplate TargetType="ComboBox">
                                            <Grid>
                                                <Border Background="White" CornerRadius="8">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition/>
                                                            <ColumnDefinition Width="20"/>
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock Text="✍️ Tác giả" Foreground="Gray" VerticalAlignment="Center" Margin="10,0" IsHitTestVisible="False">
                                                            <TextBlock.Style>
                                                                <Style TargetType="TextBlock">
                                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding SelectedIndex, RelativeSource={RelativeSource TemplatedParent}}" Value="-1">
                                                                            <Setter Property="Visibility" Value="Visible"/>
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </TextBlock.Style>
                                                        </TextBlock>
                                                        <ContentPresenter Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" VerticalAlignment="Center" Margin="10,0"/>
                                                        <TextBlock Grid.Column="1" Text="▼" FontSize="10" VerticalAlignment="Center" Foreground="Gray"/>
                                                    </Grid>
                                                </Border>
                                                <Popup Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                                    <Grid Name="DropDown" SnapsToDevicePixels="True" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                                        <Border Name="DropDownBorder" Background="White" BorderThickness="1" BorderBrush="#DDD" CornerRadius="5"/>
                                                        <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                                            <StackPanel IsItemsHost="True"/>
                                                        </ScrollViewer>
                                                    </Grid>
                                                </Popup>
                                                <ToggleButton IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" Opacity="0"/>
                                            </Grid>
                                        </ControlTemplate>
                                    </ComboBox.Template>
                                </ComboBox>

                                <ComboBox Style="{StaticResource FilterCombo}" ItemsSource="{Binding PublisherList}" SelectedItem="{Binding SelectedPublisher, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" DisplayMemberPath="Name">
                                    <ComboBox.Template>
                                        <ControlTemplate TargetType="ComboBox">
                                            <Grid>
                                                <Border Background="White" CornerRadius="8">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition/>
                                                            <ColumnDefinition Width="20"/>
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock Text="🏢 NXB" Foreground="Gray" VerticalAlignment="Center" Margin="10,0" IsHitTestVisible="False">
                                                            <TextBlock.Style>
                                                                <Style TargetType="TextBlock">
                                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding SelectedIndex, RelativeSource={RelativeSource TemplatedParent}}" Value="-1">
                                                                            <Setter Property="Visibility" Value="Visible"/>
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </TextBlock.Style>
                                                        </TextBlock>
                                                        <ContentPresenter Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" VerticalAlignment="Center" Margin="10,0"/>
                                                        <TextBlock Grid.Column="1" Text="▼" FontSize="10" VerticalAlignment="Center" Foreground="Gray"/>
                                                    </Grid>
                                                </Border>
                                                <Popup Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                                    <Grid Name="DropDown" SnapsToDevicePixels="True" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                                        <Border Name="DropDownBorder" Background="White" BorderThickness="1" BorderBrush="#DDD" CornerRadius="5"/>
                                                        <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                                            <StackPanel IsItemsHost="True"/>
                                                        </ScrollViewer>
                                                    </Grid>
                                                </Popup>
                                                <ToggleButton IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" Opacity="0"/>
                                            </Grid>
                                        </ControlTemplate>
                                    </ComboBox.Template>
                                </ComboBox>

                                <Button Command="{Binding ClearFilterCommand}" Background="Transparent" Padding="5,0" Cursor="Hand">
                                    <TextBlock Text="❌ Xóa lọc" Foreground="{StaticResource DangerColor}" FontWeight="SemiBold"/>
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                            </DockPanel>
                        </StackPanel>

                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                            <ItemsControl ItemsSource="{Binding ProductList}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel Orientation="Horizontal" ItemWidth="200" ItemHeight="280"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="White" CornerRadius="10" Margin="0,0,15,15" Width="185" Height="265">
                                            <Border.Effect>
                                                <DropShadowEffect BlurRadius="10" Opacity="0.1" ShadowDepth="2" Direction="270"/>
                                            </Border.Effect>
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="3*"/>
                                                    <RowDefinition Height="2*"/>
                                                </Grid.RowDefinitions>
                                                <Border Grid.Row="0" CornerRadius="10,10,0,0" Background="#FAFAFA" ClipToBounds="True">
                                                    <Image Source="{Binding FullImageSource}" Stretch="Uniform" Margin="10"/>
                                                </Border>
                                                <Border Grid.Row="0" Background="{StaticResource PrimaryColor}" HorizontalAlignment="Right" VerticalAlignment="Top" CornerRadius="0,10,0,10" Padding="8,4" Margin="0">
                                                    <TextBlock Text="{Binding Quantity, StringFormat='Kho: {0}'}" Foreground="White" FontSize="10" FontWeight="Bold"/>
                                                </Border>
                                                <StackPanel Grid.Row="1" Margin="10">
                                                    <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="14" TextTrimming="CharacterEllipsis" ToolTip="{Binding Title}" Height="20"/>
                                                    <TextBlock Text="{Binding AuthorName}" FontSize="11" Foreground="Gray" Margin="0,2,0,5"/>
                                                    <DockPanel LastChildFill="False" VerticalAlignment="Bottom" Margin="0,10,0,0">
                                                        <TextBlock Text="{Binding Price, StringFormat='N0'}" Foreground="{StaticResource SuccessColor}" FontWeight="Bold" FontSize="15" VerticalAlignment="Center"/>
                                                        <TextBlock Text="₫" Foreground="{StaticResource SuccessColor}" FontSize="12" VerticalAlignment="Top"/>
                                                        <Button DockPanel.Dock="Right" Command="{Binding DataContext.AddToCartCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Width="35" Height="35" Background="{StaticResource AccentColor}">
                                                            <TextBlock Text="+" FontSize="20" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-2,0,0"/>
                                                            <Button.Template>
                                                                <ControlTemplate TargetType="Button">
                                                                    <Border Background="{TemplateBinding Background}" CornerRadius="17.5">
                                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                    </Border>
                                                                </ControlTemplate>
                                                            </Button.Template>
                                                        </Button>
                                                    </DockPanel>
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>

                    <Border Grid.Column="1" Background="White" CornerRadius="10">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="20" ShadowDepth="-5" Opacity="0.1"/>
                        </Border.Effect>
                        <Grid Margin="20">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <DockPanel Grid.Row="0" Margin="0,0,0,15">
                                <StackPanel>
                                    <TextBlock Text="GIỎ HÀNG" FontWeight="Bold" FontSize="18" Foreground="{StaticResource PrimaryColor}"/>
                                    <TextBlock Text="Đơn hàng mới" Foreground="Gray" FontSize="12"/>
                                </StackPanel>
                                <Button DockPanel.Dock="Right" Content="Xóa hết" Command="{Binding ClearCartCommand}" Foreground="{StaticResource DangerColor}" Background="Transparent" BorderThickness="0" Cursor="Hand"/>
                            </DockPanel>

                            <ListView Grid.Row="1" ItemsSource="{Binding CartItems}" BorderThickness="0" Background="Transparent" Margin="-5,0">
                                <ListView.ItemContainerStyle>
                                    <Style TargetType="ListViewItem">
                                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ListViewItem">
                                                    <Border Background="{TemplateBinding Background}" BorderBrush="#F0F0F0" BorderThickness="0,0,0,1" Padding="5,8">
                                                        <ContentPresenter/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ListView.ItemContainerStyle>
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="0,5">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="70"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0">
                                                <TextBlock Text="{Binding Title}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                                                <TextBlock Text="{Binding Price, StringFormat='{}{0:N0} ₫'}" Foreground="Gray" FontSize="11"/>
                                            </StackPanel>
                                            <Border Grid.Column="1" CornerRadius="4" BorderBrush="#DDD" BorderThickness="1" Margin="10,0">
                                                <StackPanel Orientation="Horizontal">
                                                    <Button Content="-" Command="{Binding DataContext.DecreaseQtyCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Width="25" Background="Transparent" BorderThickness="0"/>
                                                    <TextBlock Text="{Binding Quantity}" Width="25" TextAlignment="Center" VerticalAlignment="Center"/>
                                                    <Button Content="+" Command="{Binding DataContext.IncreaseQtyCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Width="25" Background="Transparent" BorderThickness="0" Foreground="{StaticResource AccentColor}"/>
                                                </StackPanel>
                                            </Border>
                                            <StackPanel Grid.Column="2" HorizontalAlignment="Right">
                                                <TextBlock Text="{Binding SubTotal, StringFormat='N0'}" FontWeight="Bold"/>
                                                <Button Content="xóa" Command="{Binding DataContext.RemoveFromCartCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Foreground="{StaticResource DangerColor}" Background="Transparent" BorderThickness="0" FontSize="10" HorizontalAlignment="Right"/>
                                            </StackPanel>
                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>

                            <StackPanel Grid.Row="2" Margin="0,15,0,0">
                                <Separator Background="#EEE" Margin="0,0,0,15"/>
                                <DockPanel Margin="0,0,0,20">
                                    <TextBlock Text="TỔNG CỘNG" FontWeight="Bold"/>
                                    <TextBlock Text="{Binding CartTotal, StringFormat='N0 ₫'}" Foreground="{StaticResource SuccessColor}" FontSize="24" FontWeight="Bold" HorizontalAlignment="Right"/>
                                </DockPanel>
                                <Button Content="THANH TOÁN" Command="{Binding CheckoutCommand}" Height="50" Background="{StaticResource PrimaryColor}" Foreground="White" FontWeight="Bold" FontSize="16">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Opacity" Value="0.9"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                            </StackPanel>
                        </Grid>
                    </Border>
                </Grid>
            </TabItem>

            <TabItem Header="LỊCH SỬ">
                <Grid Margin="0,20,0,0">
                    <DataGrid ItemsSource="{Binding OrderList}" 
                              SelectedItem="{Binding SelectedOrder}" 
                              AutoGenerateColumns="False" 
                              IsReadOnly="True" 
                              Background="White" 
                              BorderThickness="0" 
                              RowHeight="40" 
                              GridLinesVisibility="Horizontal">
                        <DataGrid.Resources>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#F8F9FA"/>
                                <Setter Property="Padding" Value="10"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="BorderBrush" Value="#DDD"/>
                            </Style>
                        </DataGrid.Resources>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Mã HĐ" Binding="{Binding Id}" Width="80"/>
                            <DataGridTextColumn Header="Ngày lập" Binding="{Binding OrderDate, StringFormat='dd/MM/yyyy HH:mm'}" Width="150"/>
                            <DataGridTextColumn Header="Tổng tiền (VNĐ)" Binding="{Binding TotalAmount, StringFormat='N0'}" Width="*">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontWeight" Value="Bold"/>
                                        <Setter Property="Foreground" Value="{StaticResource SuccessColor}"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
        </TabControl>

        <Grid Background="#66000000" Visibility="{Binding IsPopupOpen, Converter={StaticResource BoolToVis}}">
            <Border Background="White" Width="700" Height="500" CornerRadius="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="{StaticResource PrimaryColor}" CornerRadius="10,10,0,0" Padding="20">
                        <DockPanel>
                            <TextBlock Text="CHI TIẾT HÓA ĐƠN" Foreground="White" FontWeight="Bold" FontSize="18"/>
                            <Button DockPanel.Dock="Right" Content="✕" Command="{Binding ClosePopupCommand}" 
                                    Background="Transparent" Foreground="White" BorderThickness="0" 
                                    FontSize="18" FontWeight="Bold" Cursor="Hand" Margin="0,-5,-5,0"/>
                        </DockPanel>
                    </Border>

                    <DataGrid Grid.Row="1" ItemsSource="{Binding OrderDetailList}" AutoGenerateColumns="False" IsReadOnly="True" Margin="20" BorderThickness="1" BorderBrush="#EEE">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Tên sách" Binding="{Binding BookTitle}" Width="*"/>
                            <DataGridTextColumn Header="Số lượng" Binding="{Binding Quantity}" Width="100"/>
                            <DataGridTextColumn Header="Đơn giá" Binding="{Binding Price, StringFormat='N0'}" Width="120"/>
                            <DataGridTextColumn Header="Thành tiền" Binding="{Binding SubTotal, StringFormat='N0'}" Width="150" FontWeight="Bold"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <Border Grid.Row="2" Padding="20" Background="#F8F9FA" CornerRadius="0,0,10,10">
                        <Button Content="Đóng" Command="{Binding ClosePopupCommand}" HorizontalAlignment="Right" Width="100" Height="35" Background="{StaticResource DangerColor}" Foreground="White" FontWeight="Bold">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="5">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </Border>
                </Grid>
            </Border>
        </Grid>

    </Grid>
</UserControl>