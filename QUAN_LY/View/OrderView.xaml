<UserControl x:Class="QUAN_LY.View.OrderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QUAN_LY.View"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1100">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <Style x:Key="SearchTextBoxStyle" TargetType="TextBox">
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" BorderBrush="#D5DBDB" BorderThickness="1" CornerRadius="5" Background="{TemplateBinding Background}">
                            <Grid>
                                <TextBlock x:Name="placeholder" Text="{TemplateBinding Tag}" Margin="10,0"
                                           VerticalAlignment="Center" Foreground="Gray" IsHitTestVisible="False"
                                           Visibility="Collapsed"/>
                                <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="placeholder" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#3498DB"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="1.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="#F4F7F9">
        <DockPanel Margin="20">
            <StackPanel DockPanel.Dock="Top" Margin="0,0,0,15">
                <TextBlock Text="LỊCH SỬ BÁN HÀNG" FontSize="24" FontWeight="Bold" Foreground="#2C3E50"/>
                <Grid Margin="0,10,0,0">
                    <TextBox Width="350" HorizontalAlignment="Left" 
                             Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}"
                             Tag="Nhập mã đơn hoặc tên nhân viên..."
                             Style="{StaticResource SearchTextBoxStyle}"/>
                </Grid>
            </StackPanel>

            <Border Background="White" CornerRadius="10" Padding="10">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="15" ShadowDepth="1" Opacity="0.1"/>
                </Border.Effect>
                <DataGrid ItemsSource="{Binding OrderList}" 
                          SelectedItem="{Binding SelectedOrder}"
                          AutoGenerateColumns="False" 
                          IsReadOnly="True" 
                          GridLinesVisibility="None"
                          HeadersVisibility="Column"
                          RowHeight="40">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Mã HĐ" Binding="{Binding Id}" Width="80"/>
                        <DataGridTextColumn Header="Ngày Lập" Binding="{Binding OrderDate, StringFormat='dd/MM/yyyy HH:mm'}" Width="160"/>
                        <DataGridTextColumn Header="Nhân Viên Bán" Binding="{Binding StaffName}" Width="180"/>
                        <DataGridTextColumn Header="Tổng Tiền" Binding="{Binding TotalAmount, StringFormat='N0'}" Width="150">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="Foreground" Value="#27AE60"/>
                                    <Setter Property="TextAlignment" Value="Right"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="Margin" Value="0,0,15,0"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Ghi Chú" Binding="{Binding Note}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </DockPanel>

        <Grid Background="#66000000" Visibility="{Binding IsPopupOpen, Converter={StaticResource BoolToVis}}">
            <Border Background="White" Width="700" Height="450" CornerRadius="15" VerticalAlignment="Center">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="30" Opacity="0.3"/>
                </Border.Effect>
                <DockPanel>
                    <Border DockPanel.Dock="Top" Background="#3498DB" CornerRadius="15,15,0,0" Padding="20,15">
                        <TextBlock Text="CHI TIẾT ĐƠN HÀNG" Foreground="White" FontWeight="Bold" FontSize="18"/>
                    </Border>

                    <UniformGrid DockPanel.Dock="Bottom" Columns="2" Margin="10">
                        <TextBlock VerticalAlignment="Center" Margin="10,0">
                            <Run Text="Trạng thái: " Foreground="Gray"/>
                            <Run Text="Hoàn tất" FontWeight="Bold" Foreground="#27AE60"/>
                        </TextBlock>
                        <Button Content="ĐÓNG CỬA SỔ" 
                                Command="{Binding ClosePopupCommand}" 
                                Width="150" HorizontalAlignment="Right"
                                Margin="15" Background="#95A5A6" Foreground="White" BorderThickness="0">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="5"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                    </UniformGrid>

                    <DataGrid Margin="15" ItemsSource="{Binding OrderDetailList}" 
                              AutoGenerateColumns="False" IsReadOnly="True" 
                              Background="Transparent" BorderThickness="0.5" BorderBrush="#EBEDEF">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Tên Sách" Binding="{Binding BookTitle}" Width="*"/>
                            <DataGridTextColumn Header="Số Lượng" Binding="{Binding Quantity}" Width="80"/>
                            <DataGridTextColumn Header="Đơn Giá" Binding="{Binding Price, StringFormat='N0'}" Width="120"/>
                            <DataGridTextColumn Header="Thành Tiền" Binding="{Binding SubTotal, StringFormat='N0'}" Width="120">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                        <Setter Property="TextAlignment" Value="Right"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>